<div class="container">
  <div class="row">
    <%= flash[:successcreate] %>
    <%= flash[:successupdate] %>
    <%= form_for(@book) do |f| %>
      <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <% end %>
</div>
  <div class="row">
	<div class="col-lg-3">
      <%= render 'share/sidebar', {user: current_user, book: @book} %>
    </div>
    <div class="col-lg-9">
      <%= render 'share/indexbook', books: @books %>
    </div>
  </div>
</div>