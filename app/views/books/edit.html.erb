<div class="container">
  <div class="row">
  	<div class="col-lg-3">
  	</div>
  	<div class="col-lg-6">
      <%= form_for(@book) do |f| %>
　　
　　　<h1>Editing Book</h1>
        <% if @book.errors.any? %>
      <div id="error_explanation">
        <p><%= @book.errors.count %>errors prohibited this obj from being saved:
        </p>
        <ul>
          <% @book.errors.full_messages.each do |message| %>
          <%= message %>
          <% end %>
        </ul>
      </div>
      <% end %>



      <h1>Editing Book</h1>
      <div class="form-group">
        <label>Title</label>
        <%= f.text_field :title, class:"form-control"  %>
      </div>

      <div class="form-group">
        <label>Opinion</label>
        <%= f.text_area :body, class:"form-control" %>
      </div>

        <%= f.submit "Update Book", class:"btn btn-primary btn-block" %>

      <% end %>


    <%= link_to "show", book_path %>|
    <%= link_to "Back", books_path %>


    </div>

    <div class="col-lg-3">
  	</div>

</div>
</div>